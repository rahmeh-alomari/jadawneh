import { ValidateInputDirectiveDirective } from './validate-input-directive.directive';

describe('ValidateInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
