import { DynamicMetaDirective } from './dynamic-meta.directive';

describe('DynamicMetaDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicMetaDirective();
    expect(directive).toBeTruthy();
  });
});
